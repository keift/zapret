name: Tests

on:
  push:
    branches:
      - '**'

jobs:
  job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tool
        run: bash ./src/install.sh --debug
